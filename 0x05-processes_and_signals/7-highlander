#!/usr/bin/env bash
# A Bash script that handles SIGTERM and keeps running

handle_sigterm() {
  echo "I am invincible!!!"
}

trap handle_sigterm SIGTERM

while true
do
  echo "To infinity and beyond"
  sleep 2
done
