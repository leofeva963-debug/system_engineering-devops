#!/usr/bin/env bash
# A Bash script that displays a list of all currently running processes
# including processes from all users, processes without a TTY, in a
# human-readable format, and showing the process hierarchy.

ps auxf
